const namespace = `http://www.w3.org/ns/shapetrees#`;

export const ST = {
  Container: `${namespace}Container`,
  Resource: `${namespace}Resource`,
  ShapeTree: `${namespace}ShapeTree`,
  contains: `${namespace}contains`,
  expectsType: `${namespace}expectsType`,
  shape: `${namespace}shape`,
  PREFIX_AND_NAMESPACE: {
    st: namespace,
  },
};
